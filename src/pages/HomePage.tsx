function HomePage() {
  return (
    <>
      <h1>Welcome to home page!</h1>
      <p>
        This template provides a minimal setup to kickstart a React app with
        TypeScript + Material UI, which use Webpack + Babel as build tool with
        configs.
      </p>
    </>
  );
}
export default HomePage;
